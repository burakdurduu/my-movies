<!DOCTYPE html>
<html lang="tr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Movies</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
  <link rel="stylesheet" href="styles/main.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <nav class="navbar fixed-top">
    <a class="logo" href="/">
      <p class="text-gold small mt-1"><i class="fa-solid fa-film"> My Movies</i></p>
    </a>
    <form action="/search" method="POST" class="d-flex navbar-search">
      <input type="text" name="movie_name" class="form-control search-input me-2" placeholder="Search movie..."
        required>
      <button class="btn btn-gold" type="submit">
        <i class="fas fa-search"></i>
      </button>
    </form>
    <div class="ml-auto">
      <% if (!locals.user) { %>
        <a class="btn btn-gold" href="/register" role="button">Register</a>
        <a class="btn btn-gold" href="/login" role="button">Login</a>
        <% } else {%>
          <a class="btn btn-gold" href="/logout" role="button">Logout</a>
          <% } %>
    </div>
  </nav>